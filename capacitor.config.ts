import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Mikicoin1311.Newwebsitetoapp',
  appName: 'MikiCoins',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
